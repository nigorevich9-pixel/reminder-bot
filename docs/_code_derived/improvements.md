# reminder-bot — improvements

- Синхронизировать UX команд с реальной семантикой core (`hold` vs pause/stop).
- Добавить команду `/projects` и выбор проекта в `/core` (передавать `project_id`).
- Добавить “переотправить статус” и “показать детали задачи” (просмотр последних task_details).
- Добавить rate-limit/anti-spam на создание задач.

